apiVersion: v1
kind: Pod
metadata:
  name: demo-crashloopbackoff
  labels:
    demo: kwatch
spec:
  restartPolicy: Always
  containers:
    - name: app
      image: busybox:1.36
      command: ["sh", "-c", "echo starting up; echo 'FATAL: cannot connect to database' >&2; exit 1"]
